export enum FetchInitCredentialsTypes {
    OMIT = 'omit',
    SAME_ORIGIN = 'same-origin',
    INCLUDE = 'include'
}

export enum HttpServiceReturnTypes {
    BLOB = 'blob',
    TEXT = 'text',
    JSON = 'json'
}

export enum RequestStatuses {
    LOADING = 'loading',
    LOADED = 'loaded',
    FAILED = 'failed'
}
